import { SectionHeaderRoot } from '@/components/section-header/root'
import { SectionHeaderLabel } from './label'
import { SectionHeaderTitle } from './title'
import { SectionHeaderDescription } from './description'

export const SectionHeader = {
  Root: SectionHeaderRoot,
  Label: SectionHeaderLabel,
  Title: SectionHeaderTitle,
  Description: SectionHeaderDescription,
}
